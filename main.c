#include <stdio.h>
#include "my_mat.h"

int main()
{
    char functionName;
    scanf("%c", &functionName);
    while (functionName != 'D')
    {
        if (functionName == 'A')
        {
            /* code */
        }
        if (functionName == 'B')
        {
            /* code */
        }
        if (functionName == 'C')
        {
            /* code */
        }
    }
}